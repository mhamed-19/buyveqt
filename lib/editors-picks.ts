// Editorially curated picks — shown above the paths grid in the default hub view.
// Swap slugs here as the corpus evolves. Order matters: first pick surfaces first.

export const EDITORS_PICKS = [
  "veqt-decision-flowchart", // the "what should I do" interactive
  "what-is-veqt", // the foundational explainer
  "veqt-vs-xeqt", // the main editorial position (VEQT > XEQT)
] as const;
